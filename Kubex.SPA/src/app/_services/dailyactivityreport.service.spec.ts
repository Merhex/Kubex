/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DailyactivityreportService } from './dailyactivityreport.service';

describe('Service: Dailyactivityreport', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DailyactivityreportService]
    });
  });

  it('should ...', inject([DailyactivityreportService], (service: DailyactivityreportService) => {
    expect(service).toBeTruthy();
  }));
});
